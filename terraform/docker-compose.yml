services:

  terraform:
    image: hashicorp/terraform
    working_dir: /workspace    
    volumes:
      - "./project:/workspace"
      - "~/.aws:/root/.aws"
    environment:
      - "TF_VAR_vpc_id=vpc-0a1a939c8456f9258"
      - "TF_VAR_ssh_key_name=MyKey"
